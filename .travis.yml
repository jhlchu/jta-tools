language: node_js
node_js: 10
  
script:
  -	./configure && make
  #- node index.js $BASE_URL $BASE_URL_BACKUP
deploy:
  provider: firebase
  token:
    secure: "$TOKEN"
  project: "$PROJECT_ID"
#- firebase login:ci
# language: node
# deploy: firebase
#  token: $TOKEN
